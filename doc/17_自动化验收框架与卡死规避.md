# 17 自动化验收框架 & 卡死规避方法论（铲屎官疯了 v2）

> 沉淀自 2026-07 多轮 codely-unity 桥接实测。目标：让 AI 能"自己跑通关、下一关、模拟看广告、给 UI 做体检"，且不再因桥接卡死而半途崩。

---

## 一、为什么 codely 桥接会"卡死"（今晚实踩的 7 个坑）

| # | 现象 | 根因 | 卡死点 |
|---|------|------|--------|
| 1 | 点「提示」/「GM」后桥接断连、回传无日志 | `PetGameSolver` BFS 同步跑（上限 6 万状态）阻塞 Unity 主线程，桥接回传超时 | 主线程阻塞 |
| 2 | 连点两次「提示」，高亮被误判"叠加" | `UnityEngine.Object.Destroy` **延迟到帧末**执行；同段同步脚本里连点，旧 HL 还没销毁就被计数 | 帧时序 |
| 3 | 结算面板只剩黑底 / 碗会重排 | 预制体挂了 `LayoutGroup`（ResultOverlay=Vertical、BowlArea=Grid），覆盖代码里的 `anchoredPosition`/绝对定位 | prefab 配置 |
| 4 | 没操作就看到「不公平」气泡 | `EditorAutoDemo` 开发期强制 `lastFedIsFirst=false` 弹气泡，干扰验收 | 测试脚本副作用 |
| 5 | 脚本里 `FindObjectsOfType` 全场景遍历后回传失败 | 脚本执行重 + 回传采集时序竞争 | 回传超时 |
| 6 | Play 后立刻取状态/截图拿到的是 Menu 而非关卡 | `Bootstrap→MenuScene→LoadGame` 需要好几帧 | 时序等待 |
| 7 | 直接改 `isCompleted` 字段不刷新 UI | 绕过了 `onClick`/`onSelectionChanged` 真实路径 | 模拟路径错误 |
| 8 | `LoadGame` 后立刻点按钮点了个"空按钮"（首次测加空碗误报 FAIL） | `PetGameUI.FindRefs/BindButtons` 在 `Start()` 跑，比 `PetGameManager.Start→StartLevel` 设 bowls **晚半帧~一帧**；只等 `gm.GetBowls().Count>0` 就点，按钮 `onClick` 还没挂 listener | 帧时序（伪卡死） |
| 9 | `execute_csharp_script` 里引用 `SystemFontText` 反复 `Connection closed` 假成功（脚本被静默 kill、文件写不出） | 运行时编译器解析不到该自定义组件类型（跨程序集），bridge 卡死 | 回传超时 |

---

## 二、规避套路（写进肌肉记忆）

1. **断言走"写文件再 Read"，绝不在重脚本里用 `capture_logs:true`**。
   重逻辑（BFS、全场景遍历）后桥接极易断连，把结果 `File.WriteAllText("C:/Users/Administrator/WorkBuddy/Claw/temp_xx.txt", ...)` 再 `Read`。
2. **跨帧才连点**：涉及 `Destroy`/重建的连续操作，分两次 `execute_csharp_script` 调用，中间 `sleep 3` 让编辑器跑帧。
3. **运行时关掉布局组**：`FindRefs()` 里 `bowlArea.GetComponent<GridLayoutGroup>().enabled=false`；`OnWin()` 清空 `ResultOverlay` 后关掉其 `LayoutGroup`。prefab 上要么别挂、要么代码关。
4. **删 `EditorAutoDemo`**：开发期自动演示会伪造 UI 行为，验收必须靠脚本驱动真实交互。
5. **重 BFS 加护栏**：求解器限状态数（已有 6 万上限）+ 加协程分步或超时兜底，避免主线程卡死。
6. **时序等待**：`play` 后 `sleep 6~7s`（等 Menu 出现）；`LoadGame(n)` 后 `sleep 5s`（等 UI 构建）。
7. **走真实路径**：用 `btn.onClick.Invoke()` 或公开方法（如 `BuildBowls` 反射调用），不要直接改私有字段模拟状态。
8. **`LoadGame` 后等 `BindButtons` 跑完**：`PetGameUI.FindRefs/BindButtons` 在 `Start()`，比 `PetGameManager.Start→StartLevel` 设 bowls 晚半帧~一帧。只等 `gm.GetBowls().Count>0` 就点按钮会点到"还没挂 listener"的空按钮（首次测加空碗误报 FAIL）。正确做法：reload 后多 yield 几帧（或先做一步 `PourFromTo` 之类真实耗时操作让帧推进），或用"reload 写标记 / 下一调用再点"两段式。
9. **`execute_csharp_script` 里禁用 `SystemFontText`**：该自定义字体组件跨程序集、运行时编译器解析不到，引用它会导致 `Connection closed` 假成功（脚本被静默 kill、文件写不出）。按按钮定位只用 `GameObject.Find` + `UnityEngine.UI.Button`，别遍历/读 `SystemFontText` 文本；需要按文本找按钮时改用 GameObject 名或"弹窗首个子 Button"。
10. **`GameObject.Find` 大小写敏感**：`BtnHint`（大写 B/H）≠ `btnHint`（小写）。先用 `FindObjectsOfType<Button>()` 列出场景中真实按钮名再 Find，避免"找不到按钮"误判。

---

## 三、可继续开发的功能矩阵（今晚任务）

| 任务 | 内容 | 状态 |
|------|------|------|
| #52 核心循环真机验收 | 倒碗→得分→喂非首位触发「不公平」气泡（时机=碗移到被喂宠物时） | ✅ 已完成 |
| #53 IAA 道具验证 | 撤销 / 加空碗 / 洗牌：按钮可点、次数扣减、效果符合设计 | ✅ 已完成（三件均 PASS） |
| #54 广告流程 | mock 翻倍/补道具弹窗走通；列真 TTSDK 前置清单 | ✅ 已完成（①端到端 PASS，②③④同构；SDK 状态见 doc/18） |
| #55 关卡数据 | 批量生成/校验 L1-50 `.asset` 落盘 `Data/Levels`，加载正确、难度曲线符合 | ✅ 已完成（50 关校验通过，见 7/20 纪要） |
| #57 收尾 | 清 `ScriptAssemblies.bak*`、starksdk 重导验 GameFont、抖音小游戏构建试包 | 待做（bak 清理被用户拒绝保留） |
| #56 自验收闭环 | 进游戏→加载→求解器跑通关→下一关→模拟看广告→UI 体检 | 目标（L1-50 已跑通） |
| #67 UI 视觉验收 | `UIAcceptanceChecker` 自动检测纯色块按钮/越界/重叠/尺寸不一；`MakeBtn` 改复用工具栏 sprite 统一美术 | ✅ 已完成（检查器已落地，待 codely 恢复后跑） |

---

## 四、自验收闭环设计（最终目标）

```
进游戏(Menu) → LoadGame(n) → 求解器算步骤 → 脚本逐步 onClick 通关
   → 结算面板体检(标题/星级/奖励/按钮可见)
   → 点「下一关」→ 验证关卡递增
   → 故意失败/点道具→触发广告弹窗→模拟"看完"→验证奖励发放
   → UI 体检: 元素拉伸/错位/锚点/文本零宽/用错图
   → 每步截图 + 写文件断言
```

**UI 体检项（自动可断言）**：
- 文本 `RectTransform.rect.width == 0` → 零宽不可见（如之前 GM 空白）
- 子物体被 `LayoutGroup` 接管导致 `sizeDelta` 异常
- 图片 `sprite == null` 或尺寸被拉伸（`rect` 比例 ≠ 原图比例）
- 按钮 `onClick` 持久事件数为 0
- 关键面板 `activeSelf` 但子节点数为 0（只剩背景）

---

## 五、安全边界

- **跑测卡死即停**：桥接断连 / 连续 2 次回传失败 → 停止当前测试，报告已验证部分，不强求跑完。
- **编译优先于运行**：每次改代码先 `request_compile` 确认 0 错误，再进 Play。
- **重逻辑不堆在一个脚本**：BFS、遍历、销毁分开调用，单次脚本只做一件事并写文件。

---

---

## 六、UI 视觉验收标准（已实现，codely 恢复后跑）

**新增** `Assets/Editor/UIAcceptanceChecker.cs`（`UnityEditor` 依赖，仅 Editor 编译）：
- 菜单「铲屎官疯了/UI视觉验收检查」一键跑
- 或 codely 调 `UIAcceptanceChecker.CheckActiveUI()`（返回报告文本，`Debug.Log` 可见）
- 检查当前激活 UI 根（GameHUD / Canvas），四项客观检测：

| 检测项 | 标准 | 后果 |
|--------|------|------|
| ① 按钮美术引用 | 每个 `Button` 的 `Image.sprite != null`（不得用纯色块占位） | FAIL |
| ② 越界 | 元素世界 AABB 不超出 `[0,0]–[750,1334]`（容差 5px） | WARN |
| ③ 同父重叠 | 同父 `Image` 交集 > 10×10px 视为重叠（如结算"看广告"被压条） | WARN |
| ④ 同排尺寸一致 | 同父按钮高度差异 > 20px 视为不一致 | WARN |

**本轮回测已修**：`PetGameUI.MakeBtn` / `BtnWatchAd` 原 `new Color(...)` 纯色块 → 改为复用工具栏 `btnUndo.image.sprite`（item_03 美术，`Image.Type.Sliced`），统一一套、零新增资源。结算面板 / 死局弹窗 / 广告补充 / GM 面板所有按钮 now 走美术。

**迭代约定**：等资源齐了再整体换皮；届时只替换 `stdBtnSprite` 来源或 prefab，检查器标准不变。阈值（`OOB_MARGIN` / `OVERLAP_MIN` / 20px）可按真机审美微调。

*文档版本：v1.1 | 更新：2026-07-22 | 来源：codely-unity 实测踩坑 + UI 视觉验收落地*
